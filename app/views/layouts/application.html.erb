<!DOCTYPE html>
<html>
  <head>
    <title>Instagram</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
          <li class="navbar-brand"><%= link_to "Instagram", root_path %></li>
      <% if user_signed_in? %>
        <li class="active"><%= link_to "new post", new_post_path %><span class="sr-only">(current)</span></a></li>
        <li class="active"><%= link_to "sign out", destroy_user_session_path, method: :delete %></a></li>
      <% else %>
        <li class="active"><%= link_to "login", new_user_session_path %><span class="sr-only">(current)</span></a></li>
        <li class="active"><%= link_to " new register", new_user_registration_path %><span class="sr-only">(current)</span></a></li>
        <% end -%>

</nav>

    <div class="container">
<% flash.each do |name, msg| %>
   <%= content_tag :div, msg, class: [:alert, alert_for(name)]%>
<% end %>
    </div>
    <%= yield %>
  </body>
</html>
